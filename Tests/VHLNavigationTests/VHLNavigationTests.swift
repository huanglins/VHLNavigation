import XCTest
@testable import VHLNavigation

final class VHLNavigationTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(VHLNavigation().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
